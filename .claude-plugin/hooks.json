{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/src/rag_cli_plugin/hooks/slash-command-blocker.py",
            "name": "slash-command-blocker",
            "priority": 150,
            "enabled": true,
            "description": "Prevents Claude from responding to slash commands, showing only execution status"
          },
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/src/rag_cli_plugin/hooks/user-prompt-submit.py",
            "name": "user-prompt-submit",
            "priority": 100,
            "enabled": true,
            "description": "Automatically enhances queries with RAG context and multi-agent orchestration"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/src/rag_cli_plugin/hooks/response-post.py",
            "name": "response-post",
            "priority": 80,
            "enabled": true,
            "description": "Adds inline citations to Claude responses when RAG context is used"
          }
        ]
      }
    ],
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/src/rag_cli_plugin/hooks/error-handler.py",
            "name": "error-handler",
            "priority": 70,
            "enabled": true,
            "description": "Provides graceful error handling with helpful troubleshooting tips"
          },
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/src/rag_cli_plugin/hooks/plugin-state-change.py",
            "name": "plugin-state-change",
            "priority": 60,
            "enabled": true,
            "description": "Persists RAG settings across Claude Code restarts"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/src/rag_cli_plugin/hooks/document-indexing.py",
            "name": "document-indexing",
            "priority": 50,
            "enabled": false,
            "description": "Automatically indexes new or modified documents (disabled by default)"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/src/rag_cli_plugin/hooks/session-start.py",
            "name": "session-start",
            "priority": 40,
            "enabled": true,
            "description": "Initializes RAG-CLI resources when a Claude Code session starts"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/src/rag_cli_plugin/hooks/session-end.py",
            "name": "session-end",
            "priority": 30,
            "enabled": true,
            "description": "Cleanup and state persistence when Claude Code session ends"
          }
        ]
      }
    ]
  }
}

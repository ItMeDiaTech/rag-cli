# RAG-CLI Environment Variables
# Copy this file to .env and fill in your values

# Anthropic API Key (required for standalone mode, optional for Claude Code mode)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Tavily Search API Key (optional, for enhanced online research)
TAVILY_API_KEY=your_tavily_api_key_here

# RAG-CLI Operation Mode
# - claude_code: Use internal Claude interface (no API key needed)
# - standalone: Use direct API calls (requires ANTHROPIC_API_KEY)
# - hybrid: Auto-detect based on environment
RAG_CLI_MODE=claude_code

# Optional: Custom port for monitoring server (default: 9999)
# RAG_CLI_PORT=9999

# Optional: Log level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Optional: Enable/disable specific features
# ENABLE_MAF=true
# ENABLE_ONLINE_RETRIEVAL=true
# ENABLE_SEMANTIC_CACHE=true
